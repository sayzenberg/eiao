extends layout.pug

block title
  title=title

block content
  div.column
    h1
      span
        b=path
      span  has been an ordeal
    img(src=image)
    h1
      span=hits
      if hits == 1
        span  time
      else
        span  times